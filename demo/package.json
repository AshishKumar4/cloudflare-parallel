{
  "name": "cloudflare-parallel-demo-assets",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && cp ../bench-results-live.json public/bench-results-live.json",
    "dev": "tsc -p tsconfig.json --watch",
    "deploy": "npm run build && ../tests/prod/test-worker/refresh-pkg.sh && ../node_modules/.bin/wrangler deploy --config ../tests/prod/test-worker/wrangler.toml"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
