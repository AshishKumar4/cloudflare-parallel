node_modules/
dist/
*.js.map
.wrangler/
.env
.env.*
.dev.vars
*.log
.DS_Store
coverage/
# Local-build tarball used by the prod-test worker to install the library
# without recursive `file:` copying. Rebuilt by `npm pack` in the
# scripts/refresh-test-worker-tgz.sh helper.
tests/prod/test-worker/cloudflare-parallel-pkg.tgz
# `npm pack` output at the package root (used to update the prod-test
# worker's local tarball install)
cloudflare-parallel-*.tgz
examples/*/node_modules/
examples/*/dist/
examples/*/.wrangler/
bench-results.json
